
services:
  opencats:
    container_name: opencats
    build: .
    ports:
      - "4000:80"
    links:
      - opencats-db

  opencats-db:
    container_name: opencats-db
    image: mysql:5.5
    ports:
      - "3003:3306"
    volumes:
      - /var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: pwd1788open
      MYSQL_DATABASE: opencats
      MYSQL_USER: andhan
      MYSQL_PASSWORD: pwd1788open§

# optional:

  phpmyadmin:  
    container_name: phpmyadmin
    image: phpmyadmin/phpmyadmin
    ports:
      - "8090:80"
    links:
      - opencats-db:db
    environment:
      PMA_HOST: db
      PMA_USER: user-here
      PMA_PASSWORD: password-here

networks:
  opencats_network:
    driver: overlay
